import NewPathCheckpoint from 'src/components/PathCheckpoint/NewPathCheckpoint'

const NewPathCheckpointPage = ({ id }) => {
  console.log(id)
  return <NewPathCheckpoint id={id} />
}

export default NewPathCheckpointPage
